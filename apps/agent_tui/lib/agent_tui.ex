defmodule AgentTui do
  @moduledoc """
  Terminal UI application for the agent harness.

  This app depends on `:agent_core` and renders events from
  `AgentCore.EventBus`. Runtime state and tool logic stay in the core app.
  """
end
