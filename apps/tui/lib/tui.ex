defmodule Tui do
  @moduledoc """
  Terminal UI components for the agent harness.

  This application owns TermUI rendering, input, and event display. The runnable
  product app is responsible for creating agent sessions and forwarding events.
  """
end
